'use strict';

/**
 * @ngdoc function
 * @name zowberApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the zowberApp
 */
angular.module('zowberApp')
  .controller('MainCtrl',
  	function ($scope) {
  		console.log($scope);
  });